# Sepiida Agent Keys File
# Keys used by agents to push progress data
# One key per line, lines starting with # are comments

# Example agent keys below are placeholders and are ignored by the server.
# Replace them with long random values before enabling static agent keys:
agent-key-001
agent-key-002
agent-key-003
